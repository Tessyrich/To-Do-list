// import todos from './todoData.js';

// const showTodo = () => {
//   todos.sort((a, b) => a.index - b.index);
//   const todoList = document.querySelector('.todo-list');
//   todoList.innerHTML = todoApp.join('');
//   const todoApp = todos.map(
//     (todo) => `        
//         <li id=${todo.index}><input type="checkbox">${todo.description} <i class="fas fa-ellipsis-v dot main"></i><i class="fas fa-trash-alt delete main"></i> </li>        
//       `
//   );
//   todoList.innerHTML = todoApp.join('');
// };

// export default showTodo;